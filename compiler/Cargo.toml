[package]
name = "visualis-compiler"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
zip = { version = "2", default-features = false, features = ["deflate"] }
